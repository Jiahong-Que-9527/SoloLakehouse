# v2.6 Release Readiness

## Scope

v2.6 retains the protected v2.5 Compose/demo acceptance path and adds one
governance-evidence gate. It is not a claim of Object Lock/WORM enforcement,
automated catalog ingestion, production RBAC, token lifecycle management, or
regulatory compliance.

## Required evidence drill

Before a v2.6 tag, run the normal v2.5 checks and then demonstrate one complete
bundle for `fin.ecb_dax_features_gold`.

| Evidence source | Required proof |
|---|---|
| Contracts | `make validate-contracts` passes |
| OpenMetadata | The configured Trino service ingested `gold.ecb_dax_features`; the table has an owner; local-only token is configured |
| Iceberg | The table has a current snapshot and bucket-relative metadata location |
| Dagster | A successful run materialized `gold_features` |
| Audit output | The evidence command writes the canonical manifest under `lineage/` in `AUDIT_BUCKET` |

```bash
make lineage-evidence \
  DATASET_ID=fin.ecb_dax_features_gold \
  DAGSTER_RUN_ID=<successful-dagster-run-id>
```

The local deployment environment, never Git, must supply
`OPENMETADATA_TRINO_SERVICE_NAME` and `OPENMETADATA_AUTH_TOKEN`.

## Release drill record — 2026-07-31 (v2.6.1)

This is the drill of record for the `v2.6.1` tag, run after `RUNTIME_VERSION`
was corrected to match the released version. `v2.6.0` shipped with the value
defaulting to `slh-v2.5.1`, which stamped the wrong runtime version into every
evidence manifest that release produced.

| Field | Value |
|---|---|
| Dataset | `fin.ecb_dax_features_gold` |
| Runtime version | `slh-v2.6.1` |
| Dagster run | `3d2ccfad-047e-47a9-9dff-c3285a473f94` (`RUN_SUCCESS`) |
| Iceberg snapshot | `8854746967558235959` |
| OpenMetadata table | `sololakehouse-trino.iceberg.gold.ecb_dax_features` |
| Audit object | `s3://sololakehouse-audit/lineage/fin.ecb_dax_features_gold/2026-07-31/3d2ccfad-047e-47a9-9dff-c3285a473f94/manifest.json` |
| Manifest `record_sha256` | `10dc3f911b24256c8babb96c2a3d74c4571c7baf5b3cec5323031f2acc087e43` |
| Schema version | `v1` |
| Timestamp (UTC) | `2026-07-31T22:39:59Z` |

The manifest was read back from the audit bucket to confirm
`runtime_version=slh-v2.6.1`.

Preconditions verified before the drill: `make verify` reported `PASS` for all
nine services, `make validate-contracts` accepted all five contracts, and the
unit suite, `ruff`, `mypy`, and `make check-agent-docs` were green.

### Superseded drill — 2026-07-31 (v2.6.0 candidate)

Retained for history. Recorded `runtime_version=slh-v2.6.0`, which the
released `v2.6.1` no longer produces.

| Field | Value |
|---|---|
| Dataset | `fin.ecb_dax_features_gold` |
| Runtime version | `slh-v2.6.0` |
| Dagster run | `43f859de-dbfd-484d-a151-065d53c0d3fe` (`RUN_SUCCESS`) |
| Iceberg snapshot | `6578735429296675758` |
| OpenMetadata table | `sololakehouse-trino.iceberg.gold.ecb_dax_features` |
| Audit object | `s3://sololakehouse-audit/lineage/fin.ecb_dax_features_gold/2026-07-31/43f859de-dbfd-484d-a151-065d53c0d3fe/manifest.json` |
| Manifest `record_sha256` | `31e11d59074a733a8ee610a44794f39ff8315e4ba6305fbd04faea2e35916b4c` |
| Schema version | `v1` |
| Timestamp (UTC) | `2026-07-31T21:42:25Z` |

Preconditions verified before the drill: `make verify` reported `PASS` for all
nine services, `make validate-contracts` accepted all five contracts, and the
unit suite (104 tests), `ruff`, and `mypy` were green.

### Superseded drill — 2026-07-30

Retained for history. Its manifest recorded `runtime_version=slh-v2.5.1`, which
is why it is not the drill of record.

The H12 drill completed locally with the following evidence:

- The OpenMetadata Trino service `sololakehouse-trino` was created and its
  bundled ingestion runner was made available in the Compose stack.
- The `trino-gold-metadata` ingestion ran successfully with three records and
  no warnings or errors.
- The target table is present at
  `sololakehouse-trino.iceberg.gold.ecb_dax_features` and has owner
  `JiahongQue`.
- The evidence adapter now verifies this four-segment FQN, including the Trino
  catalog; its focused test suite passes.
- The successful Dagster run
  `964eea78-5af5-43c6-8015-64fbf5c699cb` produced the audit object
  `s3://sololakehouse-audit/lineage/fin.ecb_dax_features_gold/2026-07-30/964eea78-5af5-43c6-8015-64fbf5c699cb/manifest.json`.
- The manifest record digest is
  `3082805a11518894e45320786e261b2f8b449bc965df5156daa58e49b2c152f8`.

The local `.env` provides the read token for this drill. Do not commit the token
or paste it into logs, chat, documentation, or Git history.

## Release record

Record the release commit, dataset ID, Dagster run ID, Iceberg snapshot ID,
audit object path, manifest `record_sha256`, and UTC timestamp. A missing
source, token, snapshot, materialization, or audit write is a failed gate: do
not emit or claim a partial evidence bundle.

## Known limitations

The manifest is a canonical, SHA-256-bound record in a stable audit layout. It
does not itself enforce immutable object retention. OpenMetadata ingestion is an
operator prerequisite, and the token is deliberately local-only. These limits
must remain visible in release notes until their respective controls are built.
