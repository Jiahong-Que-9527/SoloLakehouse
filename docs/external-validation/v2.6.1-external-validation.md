# Block `J` External Validation Appendix (v2.6.1)

This file is a **required evidence section** of the integrated post-v2.9 gate.
It is **not** the full validator protocol.

**Primary record:** [`integrated-v2.9-external-validation.md`](integrated-v2.9-external-validation.md)

Gate owner: release readiness (`docs/v2.6-release-readiness.md`)

Block under test: **v2.6.1 Block `J`** (evidence-plane operationalization)

Historical baseline: commit [`e534c73`](https://github.com/Jiahong-Que-9527/SoloLakehouse/commit/e534c73)
(PR #49). Tag `v2.6.1` predates Block `J`.

---

## Block `J` verification

Run after the integrated bootstrap in the primary record (`make init-env`,
`make setup`, OpenMetadata token in `.env.secrets`, `make demo`).

Confirm automatic lineage evidence (no manual `make lineage-evidence` on the
happy path):

1. Open the Dagster UI (`http://localhost:3000`) and copy the latest successful
   `demo_data_flow_job` or `full_pipeline_job` run id.
2. List audit-bucket objects under `lineage/` for all five governed datasets
   (`fin.ecb_rates_bronze`, `fin.dax_daily_bronze`, `fin.ecb_rates_silver`,
   `fin.dax_daily_silver`, `fin.ecb_dax_features_gold`).
3. Confirm `make verify` reports Object Lock on `AUDIT_BUCKET`.

Optional manual fallback (should not be required on the happy path):

```bash
make lineage-evidence \
  DATASET_ID=fin.ecb_dax_features_gold \
  DAGSTER_RUN_ID=<run-id>
```

### Block `J` sign-off fields

| Field | Value |
|---|---|
| Dagster run id (demo or full pipeline) | _pending_ |
| Five governed audit manifests present | _pending_ |
| Object Lock on audit bucket | _pending_ |
| Automatic emission (no manual CLI) | _pending_ |

Copy results into the integrated sign-off table in
[`integrated-v2.9-external-validation.md`](integrated-v2.9-external-validation.md).

---

## Prerequisites not in Git

| Variable | Location | How to obtain |
|---|---|---|
| `OPENMETADATA_AUTH_TOKEN` | `.env.secrets` | OM UI token after first boot |
| `OPENMETADATA_TRINO_SERVICE_NAME` | `.env.shared` | Default `sololakehouse-trino`; confirm in OM |

After editing split env files, run `make init-env` to refresh the merged `.env`.

---

## Friction log (Block `J` history)

See the integrated friction log in
[`integrated-v2.9-external-validation.md`](integrated-v2.9-external-validation.md#friction-log).
Rows E1–E5 originated from Block `J` rehearsals; E6 documents the retired
`cp .env.example .env` bootstrap path.

---

## Maintainer Block `J` rehearsal (2026-08-01)

This section captures friction found during maintainer rehearsal **before**
external sign-off. It does **not** satisfy the external-validation gate.

| Field | Value |
|---|---|
| Environment | maintainer rehearsal (not external) |
| Acceptance baseline | `e534c73` on `main` (PR #49 merged) |
| Git state at rehearsal | merged to `main`; workspace matched `origin/main` |
| Commands run | `make test`, `make lint`, `make typecheck`, `make validate-contracts`, `make check-agent-docs`, `make verify`, `make demo` |
| `make verify` | PASS (9/9 services, Object Lock GOVERNANCE 2555d on `sololakehouse-audit`) |
| `make demo` | PASS (Gold 53 rows); Dagster run `dfe42975-d7d4-4640-99e0-e9681f64130f` |
| Five governed audit manifests | PASS — automatic `lineage_evidence_sensor` emission ~15s after run success |
| Friction fixed in rehearsal | Dagster 1.12 GraphQL type mismatch; sensor default STOPPED; `job execute` runs lacked repository origin; `init-minio.sh` `grep`/Object Lock JSON check |
| OpenMetadata prerequisite | Trino service + metadata ingestion + table owners required before evidence adapters succeed (E3) |

Maintainers use the integrated rehearsal template in the primary record before
asking an external validator to repeat the full protocol.
